# excel tab

See the `info` tab in the generated excel file;
`x / y / z` should be read as:
- `x` objects of type `sh:NodeShape`
- `y` triples with predicate `sh:path`
- `z` amount of triples in shacl file