[project]
name = "shape-gen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Koen Jacobs", email = "koen.jacobs@redpencil.io" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
shape-gen = "shape_gen:main"

[build-system]
requires = ["uv_build>=0.10.0,<0.11.0"]
build-backend = "uv_build"
